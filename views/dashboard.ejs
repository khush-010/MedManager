<!-- <%- include('partials/upperBlock') %>
    
<%- include('partials/lowerBlock') %> -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MedManager Dashboard</title>
    <link rel="stylesheet" href="css/dashboard.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <div class="dashboard-container">
        <%- include('partials/dashboardNav',{activePage: 'dashboard'}) %>
        <main class="main-content">
            <header>
                <h1>Dashboard</h1>
            </header>
            <section class="cards">
                <div class="card">
                    <h3>Inventory</h3>
                    <p>Total Products : <%= totalInventory %></p>
                    <p>Total Quantity : <%= totalQuantity %></p>
                </div>
                <div class="card">
                    <h3>Billing</h3>
                    <p>Total Bills : <%= totalBills %></p>
                    <p>Total Amount : <%= totalAmount %></p>
                </div>
                <div class="card">
                    <h3>Sales</h3>
                    <p>Total Products Sold : <%= totalBillItems %></p>
                    <% if(totalTab>0){ %>
                        <p>Total Tablets : <%= totalTab %></p>
                    <% } %>
                    <% if(totalCap>0){ %>
                        <p>Total Capsules : <%= totalCap %></p>
                    <% } %>
                    <% if(totalSyrup>0){ %>
                        <p>Total Syrup : <%= totalSyrup %></p>
                    <% } %>
                    <% if(totalCream>0){ %>
                        <p>Total Cream : <%= totalCream %></p>
                    <% } %>
                </div>
            </section>
        </main>
    </div>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
